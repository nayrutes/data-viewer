<UserControl x:Class="WorldCompanyDataViewer.Views.MainDataView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WorldCompanyDataViewer.Views"
             xmlns:viewModels="clr-namespace:WorldCompanyDataViewer.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewModels:DataViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid x:Name="categoryDataGrid" AutoGenerateColumns="True"
            EnableRowVirtualization="True"
            IsReadOnly="True"
            ItemsSource="{Binding DataEntryViewSource}" 
            Margin="13,13,13,13" RowDetailsVisibilityMode="VisibleWhenSelected">
        </DataGrid>
    </Grid>
</UserControl>
