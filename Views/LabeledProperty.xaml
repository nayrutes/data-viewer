<UserControl x:Class="WorldCompanyDataViewer.Views.LabeledProperty"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WorldCompanyDataViewer.Views"
             xmlns:models="clr-namespace:WorldCompanyDataViewer.Models"
             d:DataContext="{d:DesignInstance Type=local:SampleData, IsDesignTimeCreatable=True}"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="300" d:Background="White">
    <Grid DockPanel.Dock="Top" >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Label Grid.Column="0"  x:Name="label"  Content="{Binding LabelText, RelativeSource={RelativeSource AncestorType=UserControl}}" HorizontalAlignment="Right"/>
        <Label Grid.Column="1" Content="{Binding Content, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
    </Grid>
</UserControl>
